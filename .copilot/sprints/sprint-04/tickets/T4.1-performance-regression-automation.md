# T4.1: Performance Regression Automation

**Sprint**: 4  
**Priority**: 🔴 CRITICAL  
**Estimated Effort**: 6 hours  
**Status**: NOT STARTED  
**Assignee**: TBD

---

## Context

Sprint 3 delivered a **154× speedup** in cut selection through batch optimization. As the codebase evolves and new optimizations are added, we need automated performance regression detection to ensure we don't silently regress this performance.

**Why This Is Critical**:

- Batch cut selection changed hot path (baseline protection needed)
- Future optimizations need comparison data
- Industry standard for HPC software
- Blocks production deployment without performance monitoring

**Current State**:

- Manual benchmarking only (benches/ directory exists with some benchmarks)
- No CI integration
- No documented baseline metrics
- No regression detection

**Target State**:

- Automated Criterion benchmarks for critical operations
- CI integration with regression detection
- Documented performance baselines with hardware specs
- Regression threshold enforcement (>5% slowdown fails CI)

---

## Acceptance Criteria

### Functional Requirements

- [ ] **Given** critical operations (forward pass, backward pass, cut selection, etc.), **when** benchmarks run, **then** all complete in <30 seconds total
- [ ] **Given** benchmark results, **when** compared to baseline, **then** regression >5% is detected and reported
- [ ] **Given** new code changes, **when** CI runs, **then** benchmarks execute and compare to main branch
- [ ] **Given** reference hardware specs, **when** documented, **then** users can interpret baseline metrics

### Performance Requirements

- [ ] Benchmark suite completes in <30 seconds (fast feedback)
- [ ] Individual benchmarks have <10% variance across runs
- [ ] Benchmarks cover 80%+ of runtime (solver, cut selection, forward/backward passes)

### Documentation Requirements

- [ ] `docs/performance/PERFORMANCE-BASELINES.md` created with reference metrics
- [ ] Benchmark README in `benches/` explaining how to run and interpret
- [ ] CI configuration documented in `.github/workflows/README.md`

---

## Tasks

### Implementation

- [ ] **Task 1**: Survey existing benchmarks in `benches/` directory

  - Check what's already implemented
  - Identify gaps in critical operations coverage
  - Review benchmark structure and organization

- [ ] **Task 2**: Implement missing Criterion benchmarks

  - Forward pass (single scenario): `benches/forward_pass.rs`
  - Backward pass (single stage): `benches/backward_pass.rs`
  - Cut selection batch (10/100/1000 cuts): `benches/cut_selection.rs` (may exist, verify)
  - Subproblem solve: `benches/subproblem_solve.rs`
  - Full training iteration: `benches/full_iteration.rs`
  - Simulation pass: `benches/simulation.rs`
  - State update: `benches/state_operations.rs`
  - Cut evaluation: `benches/cut_evaluation.rs`
  - Scenario sampling: `benches/scenario_generation.rs`
  - Graph traversal: `benches/graph_operations.rs`

- [ ] **Task 3**: Configure Criterion for regression detection

  - Set baseline mode in Criterion config
  - Configure statistical significance thresholds
  - Set sample sizes for consistent results
  - Configure output format (JSON + HTML)

- [ ] **Task 4**: Create GitHub Actions workflow for benchmarks

  - File: `.github/workflows/benchmarks.yml`
  - Run on: push to main, pull requests
  - Use `cargo bench` with criterion
  - Compare against main branch baseline
  - Fail CI if regression >5%
  - Upload benchmark results as artifacts

- [ ] **Task 5**: Establish baseline metrics
  - Run benchmarks on reference hardware
  - Document hardware specs (CPU, RAM, OS)
  - Save baseline results
  - Commit baseline data to repository

### Testing

- [ ] **Test 1**: Verify benchmarks run successfully

  - `cargo bench --all` completes without errors
  - All benchmarks produce valid measurements
  - Variance is acceptable (<10%)

- [ ] **Test 2**: Verify regression detection

  - Artificially slow down a hot path
  - Confirm benchmark detects >5% regression
  - Verify CI fails appropriately

- [ ] **Test 3**: Verify benchmark determinism

  - Run same benchmark multiple times
  - Confirm consistent results (within variance)
  - No flaky benchmarks

- [ ] **Test 4**: Verify CI integration
  - Create PR with no performance changes
  - Confirm benchmarks run and pass
  - Create PR with performance regression
  - Confirm CI fails with clear message

### Documentation

- [ ] **Doc 1**: Create `docs/performance/PERFORMANCE-BASELINES.md`

  - Reference hardware specifications
  - Baseline metrics for all benchmarks
  - How to interpret benchmark results
  - When to investigate regressions

- [ ] **Doc 2**: Create `benches/README.md`

  - How to run benchmarks locally
  - How to compare against baseline
  - How to add new benchmarks
  - Performance targets for each operation

- [ ] **Doc 3**: Update `.github/workflows/README.md`

  - Document benchmark workflow
  - How to interpret CI benchmark results
  - How to update baselines when intentional

- [ ] **Doc 4**: Update main `README.md`

  - Add performance badge (if applicable)
  - Link to performance baselines documentation

- [ ] **Doc 5**: Update `CHANGELOG.md`
  - Add entry for automated performance regression detection
  - Note: Infrastructure enhancement

---

## Technical Notes

### Benchmark Structure

Use Criterion best practices:

```rust
use criterion::{black_box, criterion_group, criterion_main, Criterion, BenchmarkId};

fn forward_pass_benchmark(c: &mut Criterion) {
    let (mut sddp, saa) = create_benchmark_problem();

    c.bench_function("forward_pass_single_scenario", |b| {
        b.iter(|| {
            // Use black_box to prevent compiler optimization
            black_box(sddp.forward_pass(black_box(&saa.scenarios[0])))
        });
    });
}

criterion_group!(benches, forward_pass_benchmark);
criterion_main!(benches);
```

### Critical Operations to Benchmark

1. **Solver calls** (60-80% of runtime):

   - Subproblem construction
   - LP solve with warm-start
   - Basis extraction

2. **Cut selection** (<0.5% of runtime, but recently optimized):

   - Batch cut addition (10, 100, 1000 cuts)
   - Dominance checking
   - Cut activation/deactivation

3. **SDDP operations** (10-20% of runtime):

   - Forward pass (single scenario)
   - Backward pass (single stage)
   - Full iteration (forward + backward)

4. **Data structures** (<5% of runtime, but frequently accessed):
   - State updates
   - Cut evaluation
   - Scenario sampling

### CI Configuration Tips

```yaml
# .github/workflows/benchmarks.yml
name: Performance Benchmarks

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions-rs/toolchain@v1
      - name: Run benchmarks
        run: cargo bench --bench '*' -- --save-baseline pr
      - name: Compare to main
        run: |
          git fetch origin main
          git checkout origin/main
          cargo bench --bench '*' -- --save-baseline main
          # Use criterion-compare or custom script
          cargo bench --bench '*' -- --baseline main --baseline pr
      - name: Upload results
        uses: actions/upload-artifact@v3
        with:
          name: benchmark-results
          path: target/criterion/
```

### Regression Threshold

- **5% slowdown**: Investigate and document if intentional
- **10% slowdown**: Block merge unless justified
- **20%+ slowdown**: Critical regression, must fix

### Baseline Documentation Format

```markdown
# Performance Baselines

## Reference Hardware

- **CPU**: AMD Ryzen 9 5950X (16 cores, 32 threads)
- **RAM**: 64 GB DDR4-3600
- **OS**: Ubuntu 22.04 LTS
- **Rust**: 1.75.0
- **Date**: October 6, 2025

## Baseline Metrics

| Operation                | Median Time | 95% CI   | Throughput |
| ------------------------ | ----------- | -------- | ---------- |
| Forward Pass (single)    | 1.23 ms     | ±0.05 ms | 813 ops/s  |
| Backward Pass (single)   | 2.45 ms     | ±0.08 ms | 408 ops/s  |
| Cut Selection (100 cuts) | 45 μs       | ±2 μs    | 22K ops/s  |
| Full Iteration (2-stage) | 4.12 ms     | ±0.15 ms | 243 ops/s  |

## Interpretation

- **Median Time**: Typical execution time (50th percentile)
- **95% CI**: Confidence interval (statistical significance)
- **Throughput**: Operations per second

Regressions >5% should be investigated.
```

---

## Dependencies

- **Blocked by**: None
- **Blocks**: T4.3 (needs benchmark data), T4.5 (needs benchmark framework), T4.6 (needs benchmark framework), T4.7 (needs baseline metrics)
- **Related**: T4.5 (parallel efficiency uses benchmarks)

---

## Definition of Done

- [ ] All 10+ critical operations have Criterion benchmarks
- [ ] Benchmarks run in <30 seconds total
- [ ] CI integration working (runs on PR and main)
- [ ] Regression detection configured (>5% fails CI)
- [ ] `docs/performance/PERFORMANCE-BASELINES.md` created with reference hardware specs
- [ ] `benches/README.md` created with usage instructions
- [ ] Baseline metrics documented
- [ ] All tests passing
- [ ] Code reviewed and merged
- [ ] SPRINT-STATUS.md updated

---

## Estimated Effort Breakdown

- Survey existing benchmarks: 0.5 hours
- Implement missing benchmarks: 2.5 hours
- CI integration: 1.5 hours
- Documentation: 1 hour
- Testing and validation: 0.5 hours

**Total**: 6 hours

---

## Notes for Implementer

### Getting Started

1. Check existing benchmarks: `ls benches/`
2. Run existing benchmarks: `cargo bench`
3. Review Criterion documentation: https://bheisler.github.io/criterion.rs/book/
4. Look at `tests/fixtures/benchmarks.rs` for test problem setup

### Testing Locally

```bash
# Run all benchmarks
cargo bench

# Run specific benchmark
cargo bench --bench forward_pass

# Compare against baseline
cargo bench -- --save-baseline before
# Make changes...
cargo bench -- --baseline before

# Generate HTML report
open target/criterion/report/index.html
```

### Common Pitfalls

- **Don't forget `black_box`**: Prevents compiler from optimizing away benchmark code
- **Use realistic problem sizes**: Too small = unrealistic, too large = slow benchmarks
- **Warm up the benchmark**: Criterion does this automatically, but be aware
- **Watch for variance**: High variance indicates unstable benchmark

### Questions?

- See existing benchmarks in `benches/sddp_benchmarks.rs` for examples
- See `docs/development/TESTING.md` for testing guidelines
- Consult with Sprint Planner if blocked

---

**Status Tracking**: Update SPRINT-STATUS.md when you start, make progress, or complete this ticket.
